# Claude Code local files
.claude/

# macOS
.DS_Store

# Node / Vite
node_modules/
dist/
*.log
.vite/

# Secrets (Telegram token, admin creds, JWT secret) — keep .env.example only
.env

# Server runtime data (SQLite DB + uploaded images — not committed)
server/data.db
server/data.db-*
server/uploads/
